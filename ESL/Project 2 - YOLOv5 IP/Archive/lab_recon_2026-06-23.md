# Project 2 — Lab 偵查清單（2026-06-23）

今天目的：**刺探敵情，不動手做**。把下面的未知數查清楚 / 問清楚，回來再根據結果規劃，
確認後才實作。每項標了「怎麼查」與「這會決定什麼」。

相關背景在記憶：`project2_line_clarifications.md`（老師 LINE 澄清）、`project2_overview.md`、
`project2_task_b.md`。

---

## A. Server 環境（自己上去查）

ORT 已由助教裝好，先把細節讀清楚、確認其餘 lib 在不在。

1. **讀 ORT 的 README + setup script**
   - `cat /home/user1/esl26/lib/README_ORT_1270.md`
   - `cat /home/user1/esl26/lib/ort_1270.csh`
   - 抄下：要 setenv 哪些環境變數、include path、library path、linker flags、ORT 版本。
   - 順便確認路徑：信裡寫 `user1`，但記憶裡 home 是 `usr1` —— 實際 `ls` 一下哪個對。
   - **決定**：Virtualizer 的 TLM build setting 要怎麼填（include / lib / linker）。

2. **server 上有沒有 OpenCV？**（這是預處理放哪的關鍵）
   - `pkg-config --exists opencv4 && echo HAS_OPENCV || echo NO_OPENCV`
   - `ldconfig -p | grep -i opencv`
   - `find /usr -name "opencv2" -type d 2>/dev/null`
   - **決定**：detector 前端能不能用 OpenCV 解碼/resize；不能的話走「免解碼格式 + 手刻 resize」。

3. **README 有沒有順帶提其他 lib**（SystemC、image lib 之類）—— 有的話一起抄。

---

## B. 同學情報（問那個「已經做出來」的同學，最省事）

一句話能同時解掉好幾個未知數，比等老師快。

4. **餵進 RAM 的影像是什麼格式？** PNG（要解碼）/ BMP / 自定義 raw+表頭？
   IP 是自己解碼，還是讀已經是像素的東西？
   - **決定**：detector 前端怎麼把影像讀成 640 tensor（A 餵 PNG vs B 免解碼格式）。

5. **他有沒有用到 OpenCV？** 在哪用（host 還是 server）？ → 交叉驗證 A.2。

6. **SoC / R52 是怎麼建的？** 改現成的 A35 VDK→R52、還是重建？lab IP library 裡有什麼？
   - **決定**：我們的 SoC 建構途徑（記憶裡「到 lab 才決定 how to build」）。

7. **有沒有用獨立的 DMA？** 老師說 YOLO module「跟 DMA 很像」，可能不需要再放一顆 DMA。
   - **決定**：要不要沿用 P1 的 DMA，還是 YOLO IP 自己當 master 就夠。

---

## C. SystemC template（Task C 要塞進去的骨架）

8. **template 到底存不存在、在哪？** Moodle / server 某資料夾 / demo 當天才給？
9. **它的 socket 結構**：只有 target socket（A 模型），還是含 initiator/master socket（B 模型）？
   是不是 Cortex-R52？
   - **決定**：B 模型能不能直接照 template 走、我們要自己補多少（initiator socket、b_transport 本體）。

---

## D. 中斷接線（P1 沒驗成功的痛點）

10. **INT out pin 在 Virtualizer 裡怎麼接到 R52 的中斷輸入？** 有沒有現成接法 / 範例？
    控制暫存器（SRC/DST/START + RST in / INT out，無 SIZE）對到 bus 的方式。
    - **決定**：ISR 能不能真的被觸發（避免重蹈 P1 的 IRQ 牆）。

---

## E. Demo 機制（確認得分怎麼拿）

11. **memory viewer 看 RAM 裡的 bbox 是否真的被接受**（LINE 說可以，現場確認「怎麼看」）。
12. 評分當下要看到什麼：spec 說要用 Virtualizer GUI 看 **code execution + bus transactions**。

---

## 回來後要拍板的事（依上面結果）

- 預處理放哪 + 影像格式（A.2 + B.4/5）→ detector 前端寫法、要不要手刻 resize。
- letterbox vs 拉伸（與格式一起定）→ 影響偵測數 / demo 畫面。
- SoC 建構途徑 + 要不要 DMA（B.6/7）。
- template 缺多少 → Task C 自己補的工作量（C.8/9）。
- memory map 草稿（RAM0 / RAM1 / 各暫存器 offset / DMA 位址）—— 上面定了就能落筆。

**今天不下任何實作決定；只蒐證。**
