/***************************************************************************
 * Copyright 1996-2025 Synopsys, Inc.
 *
 * This Synopsys software and all associated documentation are proprietary
 * to Synopsys, Inc. and may only be used pursuant to the terms and
 * conditions of a written license agreement with Synopsys, Inc.
 * All other use, reproduction, modification, or distribution of the
 * Synopsys software or the associated documentation is strictly prohibited.
 ***************************************************************************/
 

/***************************************************************************
 * Generated by: TLM Creator
 ***************************************************************************/

// CHECKSUM:2a6eaba57b97ec55e2e8e3f1483197d2e6767a5d 

#include <ex_import_blockTestHarness.h>



using namespace scml2::testing;



class ex_import_blockTest : public ex_import_blockTestHarness {

  // Register the test class and all test methods
  SCML2_BEGIN_TESTS(ex_import_blockTest);
  SCML2_TEST(testAlwaysSucceeds);
  SCML2_END_TESTS();

  ex_import_blockTest() {
  }
  
  // Override this if you want a customize construction of your model under test // ! Only available when not reusing component build in test
  //virtual ex_import_block* create_model_under_test(const char* name) { // ! Only available when not reusing component build in test
  //  return new ex_import_block(name); // ! Only available when not reusing component build in test
  //}

  // Override this if you want to customize construction of your mirror model
  //virtual ex_import_blockMirrorBase* create_mirror_model(const char* name) {
  //  return new ex_import_blockMirrorBase(name);
  //}
    
  // initialize is called once at the start of the test run, after the model and
  // the mirror model have been created & connected. Initialize the models further here,
  // set up extra connections, set clock periods, ...
  virtual void initialize() {
  }
  
  // Called before running each testcase
  // Reset your model here.
  virtual void setup() {
  }
  
  // Called after running each testcase
  virtual void teardown() {
  }
  
  // Called at the end of the complete testrun
  virtual void shutdown() {
  }
  
  void testAlwaysSucceeds() {
    //enable_logging()
	SCML2_ASSERT_THAT(true, "test always succeeds");
 }  
};



//SCML2_REGISTER_TEST_GROUP_WITH_PARAMETERS(ex_import_blockTest, parameter_configurations(parameter_configuration("Default")));
SCML2_REGISTER_TEST_GROUP(ex_import_blockTest);


